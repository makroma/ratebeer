<h2>Create new membership</h2>

<%= form_for(@membership) do |b| %>
	<% if @membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@membership.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @membership.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %> 
  <%= b.select :beer_club_id, options_from_collection_for_select(@beerclubs, :id, 'to_s') %>

  <%= b.submit %>
<% end %>